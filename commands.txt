uv run walkway-monitor calibrate \
  --source "rtsp://user:password@camera.local:554/stream" \
  --frames 160


uv run walkway-monitor detect --source 'rtsp://user:password@camera.local:554/stream' --baseline data/baselines/default/baseline.npz --minimum-difference 0.08 --no-depth-alignment


uv run walkway-monitor detect \
  --source 'rtsp://...' \
  --baseline data/baselines/default/baseline.npz \
  --no-depth-alignment \
  --minimum-difference 0.01 \
  --minimum-free-width-ratio 0.55


NVC_CONFIG_PATH='data/config.json' \
NVC_BASELINES_PATH='data/baselines' \
MEDIAMTX_RTSP_URL='rtsp://127.0.0.1:8554' \
uv run walkway-server


uv run walkway-monitor detect --source 'rtsp://user:password@camera.local:554/stream' --baseline data/baselines/example/baseline.npz --minimum-difference 0.08 --no-depth-heatmaps
